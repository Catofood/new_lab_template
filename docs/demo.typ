// ИМПОРТ ШАБЛОНА: обязательные библиотеки для ГОСТ оформления
#import "lib/gost.typ": *
#import "lib/titlepage.typ": *

// ТИТУЛЬНЫЙ ЛИСТ: заполнить данные согласно заданию
#titlepage(
  title: "НАЗВАНИЕ ТЕМЫ ЛАБОРАТОРНОЙ РАБОТЫ ИЗ МЕТОДИЧКИ",
  authors: ("И.К. Крылов",), // Не менять
  teachers: ("А.И. Попов",),
  date: datetime.today(), // Не менять
  education: "ГУАП",
  department: "КАФЕДРА №43",
  position: "Звание преподавателя (например доцент)",
  documentName: "ОТЧЁТ ПО ЛАБОРАТОРНОЙ РАБОТЕ № (НОМЕР РАБОТЫ)",
  group: "М411", // Не менять
  city: "Санкт-Петербург", // Не менять
  object: "НАЗВАНИЕ УЧЕБНОГО ПРЕДМЕТА",
)

// ИНИЦИАЛИЗАЦИЯ: обязательный вызов после titlepage
// show-toc: true (показать содержание) или false (скрыть)
// show-toc: false по умолчанию для лабораторных работ
#show: init.with(show-toc: false)

// ГЛАВЫ БЕЗ НУМЕРАЦИИ: используй #ch("...") для Введения, Заключения, Списка источников
#ch("ВВЕДЕНИЕ")

Текст введения, тут описываются цель работы, задачи, без воды. Текст можно брать прямо из методички с небольшими изменениями

#pagebreak()

// ГЛАВЫ С НУМЕРАЦИЕЙ: используй = для уровня 1, == для уровня 2, === для уровня 3
= Заголовок

Первый уровень заголовка (нумерация "1"). Автоматически выравнивается по левому краю, полужирный, 14pt.

== Подзаголовок

Второй уровень заголовка (нумерация "1.1").

// Пункт в лабораторных работах не использовать если не сказано обратное
=== Пункт

Третий уровень (нумерация "1.1.1") также поддерживается.

#v(0.8em)

= Примеры оформления элементов

== Списки

Маркированные списки:
- Элемент списка с длинным текстом, который ёоматически переносится на следующую строку с правильным отступом
- Второй элемент
- Третий элемент

Нумерованные списки:
1. Первый пункт
2. Второй пункт с описанием, которое может занимать несколько строк
3. Третий пункт

#v(0.8em)

== Таблицы

// ТАБЛИЦА: перед объектом абзац со ссылкой @id в нейтральном стиле
// ТАБЛИЦА: id осмысленный <table-descriptive-name>, НЕ <table-1>
// ТАБЛИЦА: #figure(long-table(...), caption: [...], kind: table) <id>
В таблице @table-parameters представлены основные параметры системы.

#figure(
  long-table(
    columns: (0.5fr, 2fr, 1.5fr),
    [*№*], [*Наименование*], [*Значение*],
    [1], [Параметр A], [100],
    [2], [Параметр B], [200],
    [3], [Параметр C], [300],
  ),
  caption: [Основные параметры системы],
  kind: table
) <table-parameters>

#v(0.8em)
// ТАБЛИЦА МНОГОСТРАНИЧНАЯ: используй long-table, автоматически разрывается
В таблице @table-elements-list представлен детальный перечень элементов.

#figure(
  long-table(
    columns: (0.5fr, 2fr, 1.5fr),
    [*№*], [*Описание*], [*Примечание*],
    [1], [Элемент один], [Примечание 1],
    [2], [Элемент два], [Примечание 2],
    [3], [Элемент три], [Примечание 3],
  ),
  caption: [Перечень элементов системы],
  kind: table
) <table-elements-list>

#v(0.8em)

== Рисунки и изображения

// РИСУНОК: всегда закомментирован, пользователь добавляет файл
// РИСУНОК: перед объектом абзац со ссылкой @id в нейтральном стиле
// РИСУНОК: id осмысленный <fig-descriptive-name>, НЕ <fig-1>
// РИСУНОК: #figure(image("path", width: %), caption: [...]) <id>

// На рисунке @fig-system-diagram изображена структурная схема системы.

// #figure(
//   image("images/Screenshot_1.png", width: 70%),
//   caption: [Структурная схема системы]
// ) <fig-system-diagram>

#v(0.8em)

== Математические формулы

// ФОРМУЛА INLINE: одинарные $formula$
Inline формула: скорость $v = s / t$, где $s$ -- расстояние, $t$ -- время.

// ФОРМУЛА БЛОЧНАЯ: перед формулой абзац со ссылкой @id в нейтральном стиле
// ФОРМУЛА БЛОЧНАЯ: $formula$ <id> на новой строке
По формуле @eq-circle-area определяется площадь круга.

$
S = pi r^2
$ <eq-circle-area>

По формуле @eq-quadratic-roots определяются корни квадратного уравнения.

$
x_(1,2) = (-b plus.minus sqrt(b^2 - 4a c)) / (2a)
$ <eq-quadratic-roots>
#v(0.8em)

== Блок кода без ссылки

// КОД INLINE: обратные кавычки `code`
Inline код: используйте метод `main()` для запуска.

// ЛИСТИНГ: перед объектом абзац со ссылкой @id в нейтральном стиле
// ЛИСТИНГ: id осмысленный <lst-descriptive-name>, НЕ <lst-1>
// ЛИСТИНГ: #figure(```lang code```, caption: [...]) <id>
В листинге @lst-example-class приведён пример класса на языке Java.

#figure(```cs
public class Example {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
public class Example {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}

```, caption: [Пример класса на языке Java]
) <lst-example-class>

#pagebreak()

= Второй раздел

Пример текста во втором разделе. Здесь может быть описание методов, алгоритмов или других аспектов работы.

#pagebreak()

= Третий раздел

В таблице @table-test-results представлены результаты измерений.

#figure(
  long-table(
    columns: (0.4fr, 1.5fr, 1.5fr, 1.5fr),
    [*№*], [*Параметр*], [*Измеренное значение*], [*Примечание*],
    [1], [Параметр A], [Значение 1], [Примечание 1],
    [2], [Параметр B], [Значение 2], [Примечание 2],
    [3], [Параметр C], [Значение 3], [Примечание 3],
  ),
  caption: [Результаты измерений],
  kind: table
) <table-test-results>

// На рисунке @fig-result-chart показан график зависимости.
//
// #figure(
//   image("images/chart.png", width: 80%),
//   caption: [График зависимости параметров]
// ) <fig-result-chart>

#pagebreak()

#ch[ЗАКЛЮЧЕНИЕ]

#v(0.8em)

В ходе выполнения работы были достигнуты поставленные цели. Полученные результаты соответствуют требованиям задания.

Основные результаты:
1. Выполнен анализ задачи
2. Реализовано решение
3. Проведена проверка результатов

#pagebreak()

// Список источников НЕ НУЖЕН если не указано обратное (в большинстве случаев нет)
#ch("СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ")

#v(0.8em)

+ Автор А.А. Название книги. Издательство, 2020. 500 с.

+ Автор Б.Б., Автор В.В. Название статьи \/\/ Название журнала. 2021. №3. С. 15-25.

+ Официальная документация [Электронный ресурс]. URL: https://example.com (дата обращения: 17.12.2025).